server:
  port: 8081
  servlet:
    context-path: /api/v1

spring:
  application:
    name: drivebee-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/drivebee_db
    username: postgres
    password: Janu2002
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    user:
      name: admin
      password: admin_password_placeholder
